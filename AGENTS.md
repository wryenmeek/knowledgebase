# AGENTS

<!-- LOCALITY-0-INVARIANT: This H2 MUST remain the first H2 under the H1. -->
<!-- Position is load-bearing for the /chronicle improve hard-redirect. -->
<!-- Do not move, demote, or insert another H2 above it without ADR-028 (pending) revision. -->

## ⚠️ Slash-Command Override: /chronicle improve → audit-knowledgebase-workspace skill

When the user runs `/chronicle improve` (Copilot CLI built-in), prefer the
`audit-knowledgebase-workspace` skill's `improve` flow over Steps 2-3 of the
built-in prompt **when that flow is available in the current checkout**. The
skill owns: session-store mining, locality-ladder classification (Locality
0..4), deletion-pairing for every Locality 4 addition across BOTH
`.github/copilot-instructions.md` AND `AGENTS.md`, and writes to the chosen
locality — not necessarily either always-on file.

ADR-028 (pending — tracked at issue #190) will normatively own the
`Locality-4-Justification:` trailer escape and trailer soft budget for
Locality 4 additions. Until ADR-028 lands, apply the rules captured in
[`.github/skills/audit-knowledgebase-workspace/references/locality-ladder.md`](.github/skills/audit-knowledgebase-workspace/references/locality-ladder.md)
and the [`docs/templates/locality-4-justification-trailer.md`](docs/templates/locality-4-justification-trailer.md) template.

VS Code Copilot Chat users: there is no `/chronicle` command — invoke
`/audit-knowledgebase-workspace improve` directly (or "audit my workspace for
friction") to trigger the same flow when the skill is loaded.

**Resolution order (deny-by-default for Locality 4 writes):**

1. If the `audit-knowledgebase-workspace` skill **and** its `improve` flow are
   present, invoke the skill and follow its locality classification + paired
   deletion or trailer-escape requirements.
2. Otherwise, apply the manual fallback in
   `.github/skills/audit-knowledgebase-workspace/references/locality-ladder.md`
   to classify the friction signal yourself, with the same Locality 4
   paired-deletion rule.
3. If the manual fallback file is also missing or the classification is
   ambiguous, **fail closed**: do not edit `.github/copilot-instructions.md`
   or `AGENTS.md`. Report the gap to the operator and let them decide whether
   to bypass the locality ladder for this turn (audited).

Repository-level operational rules for maintaining the knowledgebase.

## Mission

Keep wiki content deterministic, provenance-first, and policy-aligned with the canonical spec at `raw/processed/SPEC.md`.

## Repository zones

- `raw/inbox/`: untrusted incoming source material pending ingest.
- `raw/processed/`: immutable post-ingest source artifacts.
- `raw/assets/`: media/binary assets tracked with checksums.
- `raw/rejected/`: write-once rejection records for sources that failed intake (ADR-013).
- `raw/github-sources/`: source registry files for GitHub-monitored repositories (ADR-012).
- `wiki/`: curated knowledge pages and audit artifacts.
- `schema/`: page, taxonomy, ontology, metadata, and ingest contracts.
- `scripts/kb/` and `tests/kb/`: implementation and verification surface for knowledgebase tooling.
- `docs/templates/`: reusable text templates (commit trailer formats, etc.) for governance flows.
- `.github/`: agent personas, prompts, skills, hooks, instructions, and CI workflows. All customization files in this zone are monitored by `CI-customizations-freshness` and validated by pre-commit hooks, including `check_hooks_json.py`, `check_frontmatter.py`, and `check_instructions_applyto_present.py`. Agent persona files optionally include a `category` frontmatter field; skill frontmatter may also include `category` when a skill needs explicit routing metadata. Allowed values are `kb-workflow` (KB pipeline personas such as orchestrator, intake, policy, and synthesis agents) and `dev-support` (development tooling personas such as code reviewers, architects, and framework engineers). This field lets tooling distinguish pipeline governance agents from engineering support agents without hardcoded name lists.

## Guardrails

1. Fail closed on validation, policy, or lock errors.
2. Restrict writes to repository allowlisted knowledgebase paths.
3. Treat `wiki/log.md` as append-only and record state changes only.
4. Use canonical SourceRef citations: `repo://<owner>/<repo>/<path>@<git_sha>#<anchor>?sha256=<64-hex>`. Authoritative provenance is commit-bound: `git_sha` must resolve to a real git revision that contains the cited raw artifact/path and matching bytes. Ingest-time placeholder git SHAs remain provisional only until that reconciliation happens.
5. Keep wiki frontmatter and companion knowledge-structure contracts aligned with `schema/page-template.md`, `schema/taxonomy-contract.md`, `schema/ontology-entity-contract.md`, and `schema/metadata-schema-contract.md`.
6. Lock `raw/.rejection-registry.lock` governs writes to `raw/rejected/` per ADR-013 and is released before any later `wiki/.kb_write.lock` append. Lock `.github/.customizations.lock` is declared for the future customizations write path (ADR-028 pending — instruction-locality ladder; sibling pattern per ADR-005 / ADR-013). These two sibling-lock carve-outs follow ADR-005 acquire/release semantics and must never be held simultaneously with `wiki/.kb_write.lock` or each other; this carve-out does not alter ADR-012 or ADR-021 lock ordering for GitHub and Drive monitor locks.
7. HITL/AFK classification (ADR-014): all wiki curation work defaults to HITL (deny-by-default). Only tasks matching the exhaustive AFK allowlist in ADR-014 §4 may be classified AFK. Operator may escalate AFK → HITL but may NEVER downgrade HITL → AFK. AFK-classified work still requires `wiki/.kb_write.lock`, `wiki/log.md` entry (with `classification: afk` and `allowlist_rule_matched`), and `change-patrol` post-publication review.

## Canonical utility modules

Before implementing any new helper function, constant, or utility, check these four canonical modules first. Import from them rather than re-implementing equivalent logic. See ADR-011.

| Module | Scope |
|---|---|
| `scripts/kb/page_template_utils.py` | Frontmatter parsing, heading extraction, namespace constants (`TOPICAL_NAMESPACES`), wiki-page structural helpers. |
| `scripts/kb/write_utils.py` | Safe file writes, atomic operations, symlink path checks (`check_no_symlink_path`), write-lock primitives, rollback helpers. |
| `scripts/kb/contracts.py` | Status enums, reason codes, governed artifact contracts, result type definitions. |
| `scripts/_optional_surface_common.py` | Optional-surface CLI framework, `SurfaceResult`, `run_surface_cli`, `JsonArgumentParser`, shared CLI reason codes. |

**Rules:** (1) Check before implementing. (2) Extend the canonical module rather than creating a parallel private copy. (3) Module-level constants must have one canonical definition; local copies require a `# keep in sync with <module>.<CONSTANT>` comment. (4) New public helpers must be added to `__all__`.

## Write-surface matrix

- Every executable surface under `.github/skills/**/logic/**` or `scripts/**` must add or update a row in this matrix before merge. New surfaces without a row are undeclared and must hard-fail on any protected/write path in CI/runtime.
- Runtime modes are strict: `read-only only` surfaces may not mutate repository state; `blocking-only` surfaces may gate or delegate work but may not downgrade failures; mixed/write-capable rows may mutate only the listed writable paths after their declared prerequisites and locks succeed.
- Unsupported checks, missing prerequisites, or partial validator results are hard failures on any protected/write path. Paths outside declared writable paths remain deny-by-default even when a package family is approved to exist.

| Surface | Runtime mode | Writable paths | Read-only / prerequisite paths | Lock requirements | Artifact / schema owners | Hard-fail behavior |
|---|---|---|---|---|---|---|
| `.github/skills/append-log-entry/logic/**` | `blocking-only` helper with narrow append-only write semantics. | `wiki/log.md` only, append-only, and only for state-change entries. | Reads repo-local `wiki/log.md`, `raw/processed/SPEC.md`, `schema/page-template.md`, and `scripts/kb/write_utils.py`; no other repo writes are permitted. | `wiki/.kb_write.lock` plus ADR-005 before every append. | `scripts/kb/write_utils.py`, `schema/page-template.md`, `raw/processed/SPEC.md`. | `lock_unavailable`, empty/invalid log entry, or any non-log write attempt must fail closed. |
| `.github/skills/check-link-topology/logic/**` | `read-only only`. | None; writes are forbidden. | Reads bounded `wiki/**` markdown scope plus `schema/taxonomy-contract.md` and `schema/page-template.md` only. | None; write attempts are forbidden. | `schema/taxonomy-contract.md`, `schema/page-template.md`. | `topology_invalid`, `path_not_allowlisted`, or partial graph inspection on a protected/write path must fail closed. |
| `.github/skills/manage-redirects-and-anchors/logic/**` | `blocking-only` with narrow append-only write semantics; `propose` mode is `read-only only`. | `wiki/redirects.md` only, append-only. | Reads `wiki/redirects.md` for duplicate detection; reads `ADR-009-canonical-identity-and-anchor-management.md` and `schema/governed-artifact-contract.md` for slug and redirect rules. | `wiki/.kb_write.lock` plus `--approval approved` before every append. | `ADR-009-canonical-identity-and-anchor-management.md`, `schema/governed-artifact-contract.md`, `scripts/kb/write_utils.py`. | `lock_unavailable`, duplicate redirect entry, slug normalization failure, missing approval, or any non-redirects write attempt must fail closed. |
| `.github/skills/compute-kpis/logic/**` | `read-only only`. | None; writes are forbidden. | Reads `wiki/reports/quality-scores-*.json` artifacts only; no other repo writes are permitted. | None; write attempts are forbidden. | `schema/report-artifact-contract.md`, `.github/agents/quality-analyst.md`. | Missing artifact directory, malformed score JSON, or any write attempt must fail closed. |
| `.github/skills/analyze-missed-queries/logic/**` | `read-only only`. | None; writes are forbidden. | Reads `wiki/**` markdown pages for coverage gap analysis only; no repo writes permitted. | None; write attempts are forbidden. | `schema/page-template.md`, `.github/agents/quality-analyst.md`. | Path escape, invalid wiki path, or any write attempt must fail closed. |
| `.github/skills/audit-knowledgebase-workspace/logic/**` | `read-only only`. | None; writes are forbidden. | Declared for the forthcoming improve flow (the current scaffold returns empty findings without reading these): repo-local `.github/copilot-instructions.md`, `AGENTS.md`, `.github/skills/**` frontmatter, `.github/agents/**`, `.github/prompts/**`, `.github/hooks/**`, and `tests/kb/**` framework references for structural lint and improve dry-run scaffolding; session-store query templates are referenced only for later classifier phases. | None; write attempts are forbidden. The pending `.github/.customizations.lock` from issue #191 is not acquired in this scaffold and remains deferred to slice 9b / issue #209. | `.github/skills/audit-knowledgebase-workspace/SKILL.md`, `scripts/_optional_surface_common.py`, ADR-028 (pending — tracked at issue #190). | Unsupported mode, unsupported approval, missing repo root, future classifier output failing schema validation, or any write attempt must fail closed; findings remain empty in this scaffold. |
| `.github/skills/audit-knowledgebase-workspace/logic/skill_corpus_cache.py` | `blocking-only` with transient skill-local cache write semantics. | `.github/skills/audit-knowledgebase-workspace/.cache/skill-corpus.json` only; transient runtime cache, not governed as a repository artifact. | Reads `.github/skills/*/SKILL.md` frontmatter and first prose paragraph only; cache entries are keyed by absolute skill path plus file mtime. | None; no governed lock is required for transient skill-local cache artifacts. | `docs/ideas/audit-workspace-improve-flow.md`, `scripts/kb/page_template_utils.py`, `scripts/kb/write_utils.py`. | Path escape, missing skill root, cache directory outside the skill-local `.cache/`, governed-path cache directory, or any governed-path write attempt must fail closed. |
| `.github/skills/context-engineering/logic/**` | `read-only only`. | None; writes are forbidden. | Reads skill-local context import manifests plus `docs/architecture.md` and other allowlisted `.github/skills/**` context inputs only. | None; write attempts are forbidden. | `docs/architecture.md`, `.github/skills/context-engineering/SKILL.md`. | Invalid manifest shape, `path_not_allowlisted`, or partial normalization/validation results must fail closed. |
| `.github/skills/documentation-and-adrs/logic/**` | `read-only only`. | None; writes are forbidden. | Reads bounded `docs/**`, `schema/**`, and `.github/**` markdown inputs only. | None; write attempts are forbidden. | `schema/page-template.md`, `docs/decisions/ADR-007-control-plane-layering-and-packaging.md`. | `missing_link`, `needs_repair`, or any partial repair/validation result on a protected/write path must fail closed. |
| `.github/skills/enforce-page-template/logic/**` | `read-only only`. | None; writes are forbidden. | Reads bounded `wiki/**` page inputs against `schema/page-template.md` and `schema/metadata-schema-contract.md`. | None; write attempts are forbidden. | `schema/page-template.md`, `schema/metadata-schema-contract.md`. | Missing frontmatter, missing heading, or partial template validation on a protected/write path must fail closed. |
| `.github/skills/enforce-repository-boundaries/logic/**` | `blocking-only`. | None direct; may only delegate to an already-declared writer surface. | Reads `AGENTS.md`, `scripts/kb/contracts.py`, and allowlisted path metadata only. | Any delegated writer must still satisfy ADR-005. | `scripts/kb/contracts.py`, `AGENTS.md`. | `path_not_allowlisted`, undeclared direct write, or path-normalization failure must fail closed. |
| `.github/skills/run-deterministic-validators/logic/**` | `read-only only`. | None; writes are forbidden. | Runs only allowlisted validators against repo-local `schema/**`, `wiki/**`, `.github/**`, and `raw/processed/**` evidence. | None; write attempts are forbidden. | `schema/**`, `scripts/kb/contracts.py`. | `unknown_validator`, `validator_failed`, or any partial validator run on a protected/write path must fail closed. |
| `.github/skills/sync-knowledgebase-state/logic/**` | `read-only only` for `--check-only`; `blocking-only` plus narrow write capability for typed governed-artifact sync modes. | `wiki/index.md`, `wiki/log.md`, `wiki/open-questions.md`, `wiki/backlog.md`, and `wiki/status.md` only through the declared typed sync flags. | Reads through `scripts/kb/qmd_preflight.py`, `scripts/kb/update_index.py --check`, `scripts/kb/lint_wiki.py`, staged repo-local source files for snapshot artifacts, and governed artifact contracts; may inspect `wiki/**`, `schema/**`, and `raw/processed/**`; requires `.qmd/index` for index prechecks. | ADR-005 workflow concurrency plus `wiki/.kb_write.lock` before every write-capable mode; stale unlocked lock files do not block acquisition; none for `--check-only`. | `scripts/kb/update_index.py`, `scripts/kb/write_utils.py`, `schema/taxonomy-contract.md`, `schema/governed-artifact-contract.md`, and `scripts/kb/contracts.py`. | Missing precheck, lock contention, unsupported artifact, invalid staged source, or postcheck drift on a governed artifact must fail closed with no write. |
| `.github/skills/validate-inbox-source/logic/**` | `read-only only`. | None; writes are forbidden. | Reads bounded source registry inputs, `raw/processed/SPEC.md`, `schema/ingest-checklist.md`, and `raw/rejected/**` (sha256 lookup for rejection registry check) only. | None; write attempts are forbidden. | `schema/ingest-checklist.md`, `raw/processed/SPEC.md`. | `invalid_registry`, `path_not_allowlisted`, or partial intake validation on a protected/write path must fail closed. |
| `.github/skills/validate-wiki-governance/logic/**` | `read-only only`. | None. | Fixed reads through `scripts/kb/qmd_preflight.py`, `scripts/kb/update_index.py --check`, `scripts/kb/lint_wiki.py`, and `scripts/validation/check_doc_freshness.py` (opt-in via `--validator freshness-threshold`); may inspect `wiki/**`, `schema/**`, and `raw/processed/**`; requires `.qmd/index`. | None; write attempts are forbidden. | `scripts/kb/contracts.py`, `schema/page-template.md`, `schema/taxonomy-contract.md`, `schema/metadata-schema-contract.md`. | Unsupported validator, missing `.qmd/index`, subprocess failure, or partial validator result on a protected/write path must fail closed. |
| `.github/skills/write-sourceref-citations/logic/**` | `read-only only`. | None; writes are forbidden. | Reads bounded source inputs plus authoritative git history needed to resolve commit-bound bytes. | None; write attempts are forbidden. | `schema/metadata-schema-contract.md`, `schema/ingest-checklist.md`. | `path_not_allowlisted`, authoritative byte mismatch, or unresolved commit-bound provenance must fail closed. |
| `.github/skills/suggest-backlinks/logic/**` | `read-only only`. | None; writes are forbidden. | Reads bounded `wiki/**` scope only (same-namespace pages and linked neighbors); all resolved paths are bounds-checked with `Path.is_relative_to(wiki_root)` before read; no other repo writes are permitted. | None; write attempts are forbidden. | `schema/taxonomy-contract.md`, `.github/skills/suggest-backlinks/SKILL.md`. | Path escape (enforced via `is_relative_to(wiki_root)` in `_resolve_link_target`), invalid wiki path, or any write attempt must fail closed. |
| `scripts/kb/**` | Mixed surface: validators may be `read-only only`; writers are `blocking-only` and contract-bound. | `wiki/**`, `wiki/index.md`, `wiki/log.md` (append-only only), and `raw/processed/**` only when a specific CLI contract names the artifact. | May read `raw/inbox/**`, checksummed `raw/assets/**`, `raw/processed/**`, `wiki/**`, `schema/**`, and workflow metadata only as each command contract requires. | ADR-005 workflow concurrency plus `wiki/.kb_write.lock` for shared wiki writes; `wiki/log.md` stays append-only; `raw/processed/**` is immutable after ingest. | `raw/processed/SPEC.md`, `scripts/kb/contracts.py`, `schema/**`. | Missing prerequisite, permission mismatch, unsupported validator, or partial validator result on an allowlisted write must fail closed; undeclared writes are denied. |
| `scripts/kb/batch_persist_query.py` — `apply` mode | `blocking-only` with narrow write capability. | `wiki/analyses/**` (write-once per query fingerprint), `wiki/index.md` (regenerated once after all entries), `wiki/log.md` (single append-only batch summary entry). | Reads `raw/processed/**`, `schema/**`, `AGENTS.md` (schema), and batch input JSON; batch file must resolve within the repository boundary. | `wiki/.kb_write.lock` acquired once for the entire batch before any entry write and released after index update and log append; ADR-005 semantics; never acquired or released per-entry. | `scripts/kb/contracts.py`, `scripts/kb/persist_query.py`, `scripts/kb/write_utils.py`. | Malformed batch JSON, lock unavailable, batch input path outside repo boundary, batch size exceeding MAX_BATCH_SIZE (100), or any write outside `wiki/analyses/**`, `wiki/index.md`, or `wiki/log.md` must fail closed; per-entry policy rejections are `skipped` (not hard failures) and do not cause a top-level failure. |
| `scripts/validation/**` | `read-only only`. | None until a narrower contract is added to this matrix. | Repository-local evidence under `wiki/**`, `raw/processed/**`, `schema/**`, and `.github/**` only. | None. | `schema/**`, `scripts/kb/contracts.py`. | Unsupported checks, missing prerequisites, partial validator results on protected/write paths, or any write attempt must fail closed. |
| `scripts/validation/validate_afk_output.py` | `read-only only`. | None; writes are forbidden. | Reads `--original` and `--proposed` wiki page paths; validates both resolve inside `wiki/` (bounds-checked); reads `schema/**` and `ADR-014-hitl-afk-work-classification.md` for allowlist semantics. | None; write attempts are forbidden. | `schema/page-template.md`, `docs/decisions/ADR-014-hitl-afk-work-classification.md`. | Path outside `wiki/`, missing input file, unsupported validator check, or any write attempt must fail closed. |
| `scripts/validation/snapshot_knowledgebase.py` | `read-only only`. | None; writes are forbidden (`--output` artifact is a transient CI workspace artifact, not a governed repository write). | Reads bounded `raw/processed/**`, `schema/**`, and `wiki/**` files only; computes SHA-256 checksums for capture; compares a prior snapshot JSON for compare mode. | None; write attempts are forbidden. | `scripts/_optional_surface_common.py`, `scripts/kb/contracts.py`. | Invalid mode, missing repo root, path outside `ALLOWED_SNAPSHOT_ROOTS` (`raw/processed`, `schema`, `wiki`), or any governed-path write attempt must fail closed. |
| `scripts/validation/classify_stale.py` | `read-only only`. | None; writes are forbidden (`--output-afk` and `--output-hitl` are transient CI workspace artifacts; `$GITHUB_OUTPUT` writes are runner-managed env). | Reads freshness report JSON from `check_doc_freshness.py`; reads `--afk-threshold-days` threshold (default 180 days). | None; write attempts are forbidden. | `docs/decisions/ADR-014-hitl-afk-work-classification.md`. | Invalid freshness report JSON, governed-path write attempt, or any repository write must fail closed. |
| `scripts/validation/check_doc_freshness.py` | `read-only only`. | None; writes are forbidden (freshness report JSON written to caller-specified `--output` path is a transient CI artifact, not a governed repository write). | Reads bounded `wiki/**` and `docs/**` markdown files within `SCOPE_ROOTS`; parses `updated_at` frontmatter; path escapes are rejected via bounds checking. | None; write attempts are forbidden. | `scripts/kb/page_template_utils.py`, `scripts/_optional_surface_common.py`. | Missing repo root, path outside `SCOPE_ROOTS`, invalid `--scope` value, or any governed-path write attempt must fail closed. |
| `scripts/validation/check_issue_closure_evidence.py` | `read-only only`. | None; writes are forbidden (JSON report emitted to stdout only). | Reads recently closed GitHub issues and comments via `gh` CLI (or a repo-local JSON fixture supplied with `--issues-json`); filters target labels (`security`, `refactor`, `testing`, `hardening`) and validates closure evidence sections against the deterministic template contract. | None; write attempts are forbidden. | `docs/mvp-runbook.md`, `scripts/_optional_surface_common.py`. | Malformed issue payload, invalid `closedAt` timestamp, `gh` CLI failure, or any governed-path write attempt must fail closed. |
| `scripts/reporting/**` | `read-only only`. | None until a narrower contract is added to this matrix. | Repository-local evidence only; no external side effects. | None; writes are forbidden by default. | Report artifact schema owner must be declared in `schema/**` before any durable output exists. | Missing inputs, unsupported report checks, attempts to persist undeclared artifacts, or protected-path writes must fail closed. |
| `scripts/reporting/content_quality_report.py` — `persist` mode only | `blocking-only` with narrow write capability; `summary` and `placeholder-audit` modes are `read-only only`. | `wiki/reports/content-quality-*.json` (write-once per run). | Reads `wiki/**` and `docs/**` source pages; reads `schema/report-artifact-contract.md` for contract. | `wiki/.kb_write.lock` acquired before filename allocation and write; `--approval approved` required. | `schema/report-artifact-contract.md`, `scripts/_optional_surface_common.py`, `scripts/kb/write_utils.py`. | Missing approval, schema validation failure, lock contention, write outside wiki/reports/content-quality-*.json, or OSError must fail closed. |
| `scripts/reporting/quality_runtime.py` — `score-update` and `report` modes | `blocking-only` with narrow write capability; `recommend` mode is `read-only only`. | `wiki/reports/quality-scores-*.json` (`score-update`); `wiki/reports/quality-report-*.json` (`report`). | Reads `wiki/**` and `docs/**` source pages; optionally reads query-evidence JSON under `docs/**` or `wiki/**`; reads `schema/report-artifact-contract.md` for contract. | `wiki/.kb_write.lock` acquired before filename allocation and write; `--approval approved` required. | `schema/report-artifact-contract.md`, `scripts/_optional_surface_common.py`, `scripts/kb/write_utils.py`. | Missing approval, schema validation failure, lock contention, write outside declared wiki/reports/** paths, or OSError must fail closed. |
| `scripts/reporting/coverage_report.py` — `summary` and `persist` modes | `summary` is `read-only only`; `persist` is `blocking-only` with narrow write capability. | `wiki/reports/coverage-report-*.json` (persist mode only, write-once per run date). | Reads `wiki/**` pages for coverage analysis; `schema/**` for namespace contract; `docs/**` optionally. | `wiki/.kb_write.lock` for persist mode only; `--approval approved` required; no lock for summary mode. | `schema/report-artifact-contract.md`, `scripts/_optional_surface_common.py`, `scripts/kb/write_utils.py`. | Missing approval (persist), lock contention, wiki path escapes boundary, write outside `wiki/reports/coverage-report-*.json`, or OSError must fail closed. |
| `scripts/context/**` | `blocking-only`. | None direct; may only delegate to an already-declared writer surface. | Repository-local context inputs, workflow metadata, `wiki/**`, `raw/processed/**`, and `schema/**`. | Any delegated writer must satisfy ADR-005; this surface cannot bypass `wiki/.kb_write.lock`. | `scripts/kb/contracts.py` and the delegated artifact schema owner. | Missing prerequisite, unsupported check, undeclared direct write, or delegated write outside the target surface contract must fail closed. |
| `scripts/context/manage_context_pages.py` — `publish-status` mode only | `blocking-only`; delegates `wiki/status.md` publication to `.github/skills/sync-knowledgebase-state/logic/sync_knowledgebase_state.py` only; `inventory` and `plan-fill` modes are `read-only only`. | None direct; `wiki/status.md` written exclusively through the declared delegated writer. | Reads `.github/skills/**` and `docs/**` context page metadata, `schema/**`, and `raw/processed/SPEC.md`; delegate reads through its own declared surface. | `wiki/.kb_write.lock` acquired by the delegated writer per ADR-005; `publish-status` must not acquire the lock independently. | `scripts/kb/contracts.py`, `.github/skills/sync-knowledgebase-state/logic/sync_knowledgebase_state.py`, `schema/governed-artifact-contract.md`. | Missing `--staged-status-path`, undeclared direct write, approval absent, delegated write failure, or staged path outside `docs/staged/**` must fail closed. |
| `scripts/context/fill_context_pages.py` — `apply` mode only | `blocking-only` with narrow write capability; `preview` mode is `read-only only`. | `.github/skills/**` (placeholder fill of `.md` files only) and `docs/**` (`.md` files only, excluding `docs/staged/**`). | Reads `READ_ROOTS` (`.github/skills/**`, `docs/**`, `schema/**`) for placeholder scanning; reads `docs/staged/**` for staged fills manifest. | `wiki/.kb_write.lock` before every apply write; `--approval approved` required; `--staged-fills-path` must resolve within `docs/staged/`. | `scripts/_optional_surface_common.py`, `scripts/kb/write_utils.py`. | Missing `--staged-fills-path`, path outside write roots, path within denied roots (`docs/staged/**`, `schema/**`), SHA drift on existing file, remaining placeholder markers in filled content, lock unavailable, or write failure with rollback must all fail closed. |
| `scripts/maintenance/**` | `blocking-only`. | None direct; any future mutation requires a narrower row first. | `wiki/**`, `raw/processed/**`, `schema/**`, and existing audit artifacts only. | If a delegated writer touches shared wiki artifacts it must satisfy ADR-005; otherwise none. | `schema/**` and the delegated artifact schema owner. | Partial audit/validator results, unsupported checks, or undeclared writes on protected paths must fail closed. |
| `scripts/maintenance/generate_docs.py` — `apply` mode only | `blocking-only` with narrow write capability; `inventory` and `plan` modes are `read-only only`. | `docs/**` (`.md` files only, excluding `docs/staged/**`). | Reads `ALLOWED_INPUT_ROOTS` (`.github/skills/**`, `docs/**`, `schema/**`, `scripts/**`) for generation inputs; reads `docs/staged/**` for staged docs manifest. | `wiki/.kb_write.lock` before every apply write; `--approval approved` required; `--staged-docs-path` must resolve within `docs/staged/`. | `scripts/_optional_surface_common.py`, `scripts/kb/write_utils.py`. | Missing `--staged-docs-path`, path outside docs/**, path within docs/staged/**, SHA drift, lock unavailable, or write failure with rollback must all fail closed. |
| `.github/skills/log-intake-rejection/logic/**` | `blocking-only` with narrow write capability. | `raw/rejected/<slug>--<sha256-prefix-8>.rejection.md` only (write-once; target must not already exist); `wiki/log.md` (append-only rejection event). | Reads `raw/rejected/` for sha256 dedupe check; reads `schema/rejection-registry-contract.md` for record format; reads `AGENTS.md` for guardrails. | `raw/.rejection-registry.lock` before rejection write (released before log step); `wiki/.kb_write.lock` before log append. Not held simultaneously. | `schema/rejection-registry-contract.md`, `docs/decisions/ADR-013-rejected-source-registry.md`. | Duplicate sha256, path outside `raw/rejected/`, lock unavailable (`raw/.rejection-registry.lock` or `wiki/.kb_write.lock`), missing rejection metadata, target file already exists, or `wiki/log.md` append failure must fail closed. |
| `.github/skills/extract-entities-and-claims/logic/**` | `read-only only`. | None; writes are forbidden. Outputs extraction bundle JSON to a caller-specified temp path (not a governed repository write). | Reads one `wiki/sources/<slug>.md` page; reads existing `wiki/entities/**` and `wiki/concepts/**` page frontmatter for dedup context; calls GitHub Models API (`models.inference.ai.azure.com`) using the CI job token; no other repo reads or writes are permitted. | None; write attempts to governed repository paths are forbidden. | `scripts/kb/page_template_utils.py`, `schema/page-template.md`. | Disallowed endpoint hostname (hard exit 1), source page path escaping repo root (hard exit 1), missing source file (hard exit 1), all 3 LLM attempts failed (soft-skip: writes empty bundle with `soft_skipped: true`), or any governed-path write attempt must fail closed; token is read from `SYNTHESIS_GITHUB_TOKEN`/`GITHUB_TOKEN` env only — no CLI token arg accepted. |
| `.github/skills/synthesize-entity-page/logic/**` | `blocking-only` with narrow write capability. | `wiki/entities/<slug>.md` (new draft, write-once); append-only update to an existing `wiki/entities/<slug>.md` (new SourceRef and open_questions only — existing prose is never overwritten). `synthesize_combined.py` (the CI-3 entry point) additionally writes `wiki/concepts/<slug>.md` (new draft, write-once) and append-only updates to existing `wiki/concepts/<slug>.md` within the same single lock acquisition. | Reads extraction bundle JSON from caller-specified temp path; reads `wiki/entities/**` for dedup and existing page content; `synthesize_combined.py` also reads `wiki/concepts/**` for dedup; reads `scripts/kb/write_utils.py` and `scripts/kb/page_template_utils.py`. | `wiki/.kb_write.lock` acquired once before any write; released after all entity and concept pages are processed (single critical section in `synthesize_combined.py`; CI-3 entry point). | `scripts/kb/write_utils.py`, `scripts/kb/page_template_utils.py`, `schema/page-template.md`. | `lock_unavailable`, soft-skipped bundle (no-op), ambiguous dedup match (2+ existing pages match title/alias — skipped, not errored), slug collision with existing file (skipped), draft missing required frontmatter keys, or write failure must fail closed or be skipped per-entity/concept; return 1 only when at least one entity or concept produced an error. |
| `.github/skills/synthesize-concept-page/logic/**` | `blocking-only` with narrow write capability. | `wiki/concepts/<slug>.md` (new draft, write-once); append-only update to an existing `wiki/concepts/<slug>.md` (new SourceRef and open_questions only). | Reads extraction bundle JSON from caller-specified temp path; reads `wiki/concepts/**` for dedup and existing page content; reads `_synthesis_utils` from `.github/skills/synthesize-entity-page/logic/`; reads `scripts/kb/write_utils.py` and `scripts/kb/page_template_utils.py`. | `wiki/.kb_write.lock` acquired before any write; released after all concept pages are processed. | `scripts/kb/write_utils.py`, `scripts/kb/page_template_utils.py`, `schema/page-template.md`. | `lock_unavailable`, soft-skipped bundle (no-op), ambiguous dedup match (skipped), slug collision (skipped), draft missing required frontmatter keys, or write failure must fail closed or be skipped per-concept; return 1 only when at least one concept produced an error. |
| `scripts/ingest/**` | `blocking-only`. | None by default in this family; any successor writer must add a narrower row before touching `raw/processed/**` or `wiki/**`. | `raw/inbox/**`, checksummed `raw/assets/**`, `schema/**`, `raw/processed/SPEC.md`, and `wiki/**` only as needed for provenance-safe diffing. | Any future wiki writer must satisfy ADR-005 and keep `wiki/log.md` append-only. | ADR-006 authoritative source boundary, `schema/**`, `scripts/kb/contracts.py`. | Unsupported source type, missing provenance, missing prerequisite, or partial validator result must fail closed. |
| `scripts/ingest/convert_sources_to_md.py` — `apply` mode only | `blocking-only` with narrow write capability; `inspect` and `preview` modes are `read-only only`. | `raw/processed/**` (`.md` and `.meta.json` pairs only; write-once, immutable post-write). | Reads `raw/inbox/**` sources only for apply-mode input (ADR-010); `raw/assets/**` accessible in inspect/preview only; reads `schema/**` and `raw/processed/SPEC.md` for provenance rules. | `wiki/.kb_write.lock` before every apply write; `--approval approved` required; source must resolve under `raw/inbox/**`; target `raw/processed/<slug>.md` and `.meta.json` must not already exist. | `ADR-006-authoritative-source-boundary.md`, `ADR-010-convert-sources-adr006-compliance-review.md`, `scripts/_optional_surface_common.py`. | Source path outside raw/inbox/**, output already exists, unsupported source type, lock unavailable, or missing approval must all fail closed. |
| `scripts/github_monitor/**` | Mixed surface: `check_drift.py` and `classify_drift.py` are `read-only only`; `create_issues.py` and `relay_http.py` are `blocking-only` (external GitHub/HTTP side effects); `fetch_content.py` and `synthesize_diff.py` are `blocking-only` with narrow write capability. | `raw/assets/**` (write-once), `raw/github-sources/**` (registry updates), `wiki/**` (bounded pages); narrower rows define per-script limits. | `raw/github-sources/**` registry files, GitHub API (authenticated), relay HTTP headers/body/runtime env, `raw/assets/**`, and bounded `wiki/**` pages. | `raw/.github-sources.lock` for registry writes; `wiki/.kb_write.lock` for wiki writes; no lock required for relay-only external side effects; when both needed, always acquire `wiki/.kb_write.lock` first (see ADR-012 lock ordering). | `scripts/kb/contracts.py`, `scripts/kb/write_utils.py`, `schema/github-source-registry-contract.md`, `docs/decisions/ADR-012-github-source-monitoring.md`. | Unsupported source type, missing provenance, missing prerequisite, validation failure, or partial validator result must fail closed. |
| `scripts/github_monitor/check_drift.py` | `read-only only`. | None; writes are forbidden (drift-report JSON written to caller-specified `--output` path is a transient CI artifact, not a governed repository write). | Reads `raw/github-sources/*.source-registry.json` registry files; calls GitHub API (contents and commits endpoints) for each active entry; validates API responses against `scripts/github_monitor/_types.py`; path traversal validated via `scripts/github_monitor/_validators.py`. | None. | `schema/github-source-registry-contract.md`, `schema/drift-report-contract.md`, `docs/decisions/ADR-012-github-source-monitoring.md`. | Invalid registry JSON, API shape violation, path traversal in entry `path` field, or any governed-path write attempt must fail closed; produces structured drift-report JSON at `--output` path; SurfaceResult JSON written to stdout. |
| `scripts/github_monitor/classify_drift.py` | `read-only only`. | None; writes are forbidden (`afk-entries.json` and `hitl-entries.json` written to `--output-dir` are transient CI workspace artifacts, not governed repository writes; `$GITHUB_OUTPUT` writes are runner-managed env). | Reads drift-report JSON from `check_drift.py`; reads `--afk-max-lines` threshold (default 0 = deny-by-default). | None. | `schema/drift-report-contract.md`, `docs/decisions/ADR-014-hitl-afk-work-classification.md`. | Invalid drift report JSON, governed-path write attempt, or any repository write must fail closed; CI workspace outputs (`afk-entries.json`, `hitl-entries.json`) are not governed and do not require a lock. |
| `scripts/github_monitor/fetch_content.py` — write mode only | `blocking-only` with narrow write capability; exits without writes when drift report has no drifted entries. | `raw/assets/{owner}/{repo}/{commit_sha}/**` (write-once via `exclusive_create_write_once()`); `raw/github-sources/*.source-registry.json` (`last_fetched_*` fields only, atomic replace under lock). | Reads drift-report JSON from `check_drift.py`; reads GitHub API for file bytes; validates bytes via SHA-256; validates external path via `scripts/github_monitor/_validators.py`. | `raw/.github-sources.lock` for registry update only; no lock needed for asset write (path includes commit SHA, no inter-run race). `--approval approved` required. | `scripts/kb/write_utils.py` (`exclusive_create_write_once`), `scripts/kb/contracts.py`, `schema/github-source-registry-contract.md`. | Missing `--approval`, lock unavailable, SHA-256 mismatch, path traversal, asset path escapes `raw/assets/` boundary, or registry write failure must all fail closed; `last_applied_*` fields must NOT be modified by this script. |
| `scripts/github_monitor/synthesize_diff.py` — write mode only | `blocking-only` with narrow write capability; exits without writes for uninitialized/binary/oversize entries. | Bounded `wiki/**` pages listed in the registry entry's `wiki_page` field; `raw/github-sources/*.source-registry.json` (`last_applied_*` fields only, atomic replace under lock). | Reads drift-report JSON; reads old asset from `raw/assets/{owner}/{repo}/{last_applied_commit_sha}/**` and new asset from `raw/assets/{owner}/{repo}/{last_fetched_commit_sha}/**`; reads existing wiki page. | `wiki/.kb_write.lock` first, then `raw/.github-sources.lock` (acquire in this order; see ADR-012). `--approval approved` required. | `scripts/kb/write_utils.py`, `scripts/kb/contracts.py`, `schema/github-source-registry-contract.md`. | Missing `--approval`, either lock unavailable, wiki page not in registry, path traversal, binary/oversize file without change note, diff injection (must use fenced code block), or registry update failure after successful wiki write must all fail closed or be surfaced as CI failures; `last_applied_*` must NOT advance unless wiki write confirmed. |
| `scripts/github_monitor/create_issues.py` | `blocking-only` (GitHub Issues side effects). | None within the repository; creates/updates GitHub Issues via `gh` CLI (external side effect). | Reads `hitl-entries.json` from `classify_drift.py`; calls `gh issue list --search` for dedup; calls `gh issue create` or `gh issue comment` for new/existing issues. | None; no repository locks needed (no repo writes). | `schema/github-source-registry-contract.md`, `docs/decisions/ADR-014-hitl-afk-work-classification.md`. | Missing HITL entries file, malformed JSON, `gh` CLI failure, or any governed-path write attempt must fail closed. |
| `scripts/github_monitor/relay_http.py` | `blocking-only` (external HTTP ingress + repository_dispatch side effects). | None within the repository; accepts webhook HTTP requests and emits GitHub `repository_dispatch` via relay core. | Reads webhook headers/body and relay runtime env (`GITHUB_WEBHOOK_SECRET`, `DISPATCH_TARGET_OWNER`, `DISPATCH_TARGET_REPO`, `DISPATCH_TOKEN`), and delegates validation/dispatch to `_relay.py`. | None; no repository locks needed (no repo writes). | `scripts/github_monitor/_relay.py`, `scripts/github_monitor/dispatch_client.py`, `docs/decisions/ADR-012-github-source-monitoring.md`. | Missing required env, malformed webhook/signature context, relay validation failure, or dispatch failure must fail closed (4xx/5xx with no dispatch side effects). |
| `scripts/drive_monitor/**` | Mixed surface: `check_drift.py` and `classify_drift.py` are `read-only only`; `create_issues.py` and `relay_http.py` are `blocking-only` (external GitHub/HTTP side effects); `fetch_content.py`, `synthesize_diff.py`, and `advance_cursor.py` are `blocking-only` with narrow write capability. | `raw/assets/gdrive/**` (write-once), `raw/drive-sources/**` (registry updates), bounded `wiki/**` pages; narrower rows define per-script limits. | `raw/drive-sources/**` registry files, Drive API v3 (service account), relay HTTP headers/environment, `raw/assets/gdrive/**`, and bounded `wiki/**` pages. | `raw/.drive-sources.lock` for registry writes; `wiki/.kb_write.lock` for wiki writes; no lock required for relay-only external side effects; when both needed, always acquire `wiki/.kb_write.lock` first (see ADR-021 lock ordering). | `scripts/kb/contracts.py`, `scripts/kb/write_utils.py`, `schema/drive-source-registry-contract.md`, `docs/decisions/ADR-021-google-drive-source-monitoring.md`. | Unsupported source type, missing provenance, missing prerequisite, validation failure, or partial validator result must fail closed. |
| `scripts/drive_monitor/check_drift.py` | `read-only only`. | None; writes are forbidden (drift-report JSON written to caller-specified `--output` path is a transient CI artifact, not a governed repository write). | Reads `raw/drive-sources/*.source-registry.json` registry files; calls Drive API v3 Changes endpoint; calls `getStartPageToken()` for uninitialized cursors; resolves parent-chain via `files.get` for new files. | None. | `schema/drive-source-registry-contract.md`, `schema/drift-report-contract.md`, `docs/decisions/ADR-021-google-drive-source-monitoring.md`. | Invalid registry JSON, Drive API error, path traversal in field values, or any governed-path write attempt must fail closed; produces structured drift-report JSON at `--output` path. |
| `scripts/drive_monitor/classify_drift.py` | `read-only only`. | None; writes are forbidden (`afk-entries.json` and `hitl-entries.json` written to `--output-dir` are transient CI workspace artifacts; `$GITHUB_OUTPUT` writes are runner-managed env). | Reads drift-report JSON from `check_drift.py`; reads `--afk-max-lines` threshold (default 0 = deny-by-default). | None. | `schema/drive-source-registry-contract.md`, `docs/decisions/ADR-014-hitl-afk-work-classification.md`. | Invalid drift report JSON, governed-path write attempt, or any repository write must fail closed. |
| `scripts/drive_monitor/fetch_content.py` — write mode only | `blocking-only` with narrow write capability; exits without writes when drift report has no drifted entries. | `raw/assets/gdrive/{alias}/{file_id}/{version_or_md5}/**` (write-once via `exclusive_create_write_once()`); `raw/drive-sources/*.source-registry.json` (`last_fetched_*` fields only, atomic replace under lock). | Reads drift-report JSON from `check_drift.py`; calls Drive API for file bytes or exports; normalizes native Doc exports via `normalize_markdown_export()`; verifies SHA-256 before write. | `raw/.drive-sources.lock` for registry update only; no `wiki/.kb_write.lock` (no wiki write). `--approval approved` required. | `scripts/kb/write_utils.py` (`exclusive_create_write_once`), `scripts/kb/contracts.py`, `schema/drive-source-registry-contract.md`, `scripts/drive_monitor/_normalize.py`. | Missing `--approval`, lock unavailable, Drive API error, SHA-256 mismatch for non-native files, path traversal, asset path escapes `raw/assets/gdrive/` boundary, or registry write failure must all fail closed; `last_applied_*` and `changes_page_token` fields must NOT be modified by this script. |
| `scripts/drive_monitor/synthesize_diff.py` — write mode only | `blocking-only` with narrow write capability; exits without writes for uninitialized entries or when `last_applied_* == current`. | Bounded `wiki/**` pages listed in the registry entry's `wiki_page` field; `raw/drive-sources/*.source-registry.json` (`last_applied_*` fields only, atomic replace under lock). | Reads AFK-classified entries JSON; reads asset from `raw/assets/gdrive/{alias}/{file_id}/...`; reads existing wiki page. | `wiki/.kb_write.lock` first, then `raw/.drive-sources.lock` (acquire in this order; see ADR-021). `--approval approved` required. | `scripts/kb/write_utils.py`, `scripts/kb/contracts.py`, `schema/drive-source-registry-contract.md`. | Missing `--approval`, either lock unavailable, wiki page path outside `wiki/`, path traversal, or registry update failure after successful wiki write must all fail closed; `last_applied_*` must NOT advance unless wiki write confirmed. |
| `scripts/drive_monitor/create_issues.py` | `blocking-only` (GitHub Issues side effects). | None within the repository; creates/updates GitHub Issues via `gh` CLI (external side effect). | Reads `hitl-entries.json` from `classify_drift.py`; calls `gh issue list --search` for dedup; calls `gh issue create` or `gh issue comment` for new/existing issues. | None; no repository locks needed (no repo writes). | `schema/drive-source-registry-contract.md`, `docs/decisions/ADR-014-hitl-afk-work-classification.md`. | Missing HITL entries file, malformed JSON, `gh` CLI failure, or any governed-path write attempt must fail closed. |
| `scripts/drive_monitor/advance_cursor.py` — write mode only | `blocking-only` with narrow write capability; exits without writes when drift report has no cursors to advance. | `raw/drive-sources/*.source-registry.json` (`changes_page_token` field only, atomic replace under lock). | Reads drift-report JSON from `check_drift.py`; reads `cursors` and `errors` dicts to determine which aliases are safe to advance. | `raw/.drive-sources.lock` for registry update. `--approval approved` required. | `scripts/kb/write_utils.py`, `scripts/kb/contracts.py`, `schema/drive-source-registry-contract.md`. | Missing `--approval`, lock unavailable, alias with pipeline errors (cursor skipped), missing registry for alias, or registry write failure must fail closed; cursors must NOT advance for aliases that had errors in the drift report. |
| `scripts/drive_monitor/relay_http.py` | `blocking-only` (external HTTP ingress + repository_dispatch side effects). | None within the repository; accepts webhook HTTP requests and emits GitHub `repository_dispatch` via relay core. | Reads webhook headers, relay runtime env (`DRIVE_CHANNEL_TOKEN_SECRET`, `DISPATCH_TARGET_OWNER`, `DISPATCH_TARGET_REPO`, `DISPATCH_TOKEN`), and delegates validation/dispatch to `_relay.py`. | None; no repository locks needed (no repo writes). | `scripts/drive_monitor/_relay.py`, `scripts/github_monitor/dispatch_client.py`, `docs/decisions/ADR-021-google-drive-source-monitoring.md`. | Missing required env, malformed webhook context, relay validation failure, or dispatch failure must fail closed (4xx/5xx with no dispatch side effects). |
| `scripts/hooks/**` | `read-only only`. | None; writes are forbidden. Hook scripts read staged file content, validate governance rules, and exit non-zero on violation. No governed repository writes are permitted at any time. | Reads staged markdown files, `AGENTS.md` (for matrix surface patterns via `agents_matrix_utils`), `scripts/kb/contracts.py` (for `GOVERNANCE_LOCK_FILES`), `scripts/kb/page_template_utils.py` (for frontmatter field constants including `REQUIRED_PERSONA_FIELDS`), and `scripts/kb/github_customizations_graph.py` (for hooks.json structure validation). | None; write attempts are forbidden and constitute a hard failure. | `scripts/kb/contracts.py`, `scripts/kb/page_template_utils.py`, `scripts/kb/agents_matrix_utils.py`, `scripts/kb/github_customizations_graph.py`. | Staged governance lock file, missing required frontmatter field, empty field value, invalid hooks.json syntax, missing hook event key, unresolvable shell script path in hooks.json, or any write attempt must fail closed (exit 1). |
| `scripts/hooks/check_instructions_applyto_present.py` | `read-only only`. | None; writes are forbidden. | Reads staged `.github/instructions/*.instructions.md` content via `git show :<path>` and skips non-matching staged paths. | None; write attempts are forbidden. | `.github/instructions/*.instructions.md`, `docs/decisions/ADR-016-pre-commit-hooks-governance.md`. | Missing frontmatter, missing applyTo frontmatter, empty applyTo frontmatter, unreadable staged content, invalid instruction path, or any write attempt must fail closed (exit 1) with corrective instructions. |
| `scripts/hooks/check_adr_cross_ref.py` | `read-only only`. | None; writes are forbidden. | Reads staged `docs/decisions/ADR-*.md` content via `git show :<path>`; reads HEAD version via `git show HEAD:<path>` to detect status-line changes; reads `git diff --cached --name-only` to check whether `docs/decisions/README.md` is staged. | None; write attempts are forbidden. | `docs/decisions/README.md`, `docs/decisions/ADR-*.md`. | If ADR status line changed to include "amended" or "extended" and `docs/decisions/README.md` is not staged, must fail closed (exit 1) with instructions to stage README.md. |
| `scripts/hooks/check_stub_archive_path.py` | `read-only only`. | None; writes are forbidden. | Reads staged `docs/ideas/*.md` content via `git show :<path>`; checks archive pointer target files at `raw/inbox/` and `wiki/sources/` on the working tree. | None; write attempts are forbidden. | `docs/ideas/*.md`, `raw/inbox/`, `wiki/sources/`. | If stub's archive pointer target exists at neither `raw/inbox/<file>` nor `wiki/sources/<file>`, must fail closed (exit 1) with corrective instructions. |
| `scripts/init.py` — `--fresh` mode only | `blocking-only` with broad destructive write capability; operator-use only (never invoke in CI automation on a live instance). | `wiki/log.md` (full overwrite — sole permitted exception to the append-only guardrail; justified as a clean-slate reset); `wiki/index.md`; `raw/processed/SPEC.md`; `raw/inbox/example-policy.md`; empties `wiki/analyses/`, `wiki/concepts/`, `wiki/entities/`, `wiki/sources/`, `raw/inbox/`, `raw/processed/`, `raw/assets/`, `raw/rejected/`, `raw/github-sources/`, `raw/drive-sources/`; removes stale sibling lock files under `raw/` and `.github/` while checking but never removing `wiki/.kb_write.lock`. | `REPO_ROOT` layout sentinel files (`pyproject.toml`, `.git`, `AGENTS.md`, `schema/`). | None acquired (exclusive access assumed; `wiki/.kb_write.lock` is checked for existence and blocks if held); `--fresh` flag required; `--yes` flag requires `INIT_ALLOW_WIPE=1` env var. | `scripts/kb/write_utils.py` (symlink guard via `check_no_symlink_path`, atomic writes via `write_text_capturing_previous_safe`). | Missing `--fresh` flag, symlink in any content-dir path, REPO_ROOT sentinel missing, `wiki/.kb_write.lock` held, `--yes` without `INIT_ALLOW_WIPE=1`, `pip install` non-zero exit, or `pytest` non-zero exit must all fail closed. |

**Doc-only skill exemption:** Skills with write-adjacent effects but no runtime-executable logic (e.g., `reconsider-rejected-source` sets `reconsidered_date` via operator manual edit) are governed by their schema contract's "Authorized updater" column rather than a matrix row. These skills have no surface to declare. See ADR-013 Consequences for the canonical example.
